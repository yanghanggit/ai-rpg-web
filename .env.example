# 后端地址（FastAPI 游戏服务器）
# 本机开发用 localhost；局域网开发填本机网卡 IP，例如 http://192.168.22.235:8000
# 查看本机 IP：macOS 用 `ifconfig en0`，Linux 用 `ip addr`
VITE_API_BASE_URL=http://localhost:8000

# 可选：仅当 OpenAPI 与后端不同源时才需要设置。
# 缺省时取 ${VITE_API_BASE_URL}/openapi.json
# VITE_OPENAPI_URL=http://localhost:8000/openapi.json
